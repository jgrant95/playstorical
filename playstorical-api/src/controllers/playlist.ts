import { Request, Response, NextFunction } from "express";

// This controller will primarily be used by read reqs. from client

const capture = async (req: Request, res: Response, next: NextFunction) => {
}